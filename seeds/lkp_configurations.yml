version: 2
 
seeds:
  - name: lkp_divisions
    config:
      schema:  "{{env_var('DBT_LKPSCHEMA', 'LOOKUP_DEV')}}"

  - name: lkp_catagories
    config:
      schema : "{{env_var('DBT_LKPSCHEMA', 'LOOKUP_DEV')}}"
  
  - name : lkp_shippers
    config:
      schema : "{{env_var('DBT_LKPSCHEMA', 'LOOKUP_DEV')}}"